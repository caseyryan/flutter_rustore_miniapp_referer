name: flutter_rustore_miniapp_referrer
description: "A flutter plugin which allows to get marketing referrer from RuStore and Web App"
version: 1.0.3
homepage: https://github.com/caseyryan/flutter_rustore_miniapp_referer

environment:
  sdk: ^3.10.4
  flutter: '>=3.3.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_web_plugins:
    sdk: flutter
  web: ^1.0.0
  plugin_platform_interface: ^2.0.2
  lite_state: ^4.1.1
  json_annotation: ^4.9.0
  

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  json_serializable: ^6.11.3
  build_runner: ^2.4.4

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:
  # This section identifies this Flutter project as a plugin project.
  # The 'pluginClass' specifies the class (in Java, Kotlin, Swift, Objective-C, etc.)
  # which should be registered in the plugin registry. This is required for
  # using method channels.
  # The Android 'package' specifies package in which the registered class is.
  # This is required for using method channels on Android.
  # The 'ffiPlugin' specifies that native code should be built and bundled.
  # This is required for using `dart:ffi`.
  # All these are used by the tooling to maintain consistency when
  # adding or updating assets for this project.
  plugin:
    platforms:
      android:
        package: com.softmaestri.flutter_rustore_miniapp_referrer
        pluginClass: FlutterRustoreMiniappReferrerPlugin
      ios:
        pluginClass: FlutterRustoreMiniappReferrerPlugin
      web:
        pluginClass: FlutterRustoreMiniappReferrerWeb
        fileName: flutter_rustore_miniapp_referrer_web.dart
