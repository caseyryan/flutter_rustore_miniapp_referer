name: flutter_rustore_miniapp_referer_example
description: "Demonstrates how to use the flutter_rustore_miniapp_referer plugin."

environment:
  sdk: ^3.10.4

dependencies:
  flutter:
    sdk: flutter

  flutter_rustore_miniapp_referer:
    path: ../

  cupertino_icons: ^1.0.8

dev_dependencies:
  integration_test:
    sdk: flutter
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
